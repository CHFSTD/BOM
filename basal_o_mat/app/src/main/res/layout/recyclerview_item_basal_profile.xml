<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_marginTop="@dimen/halfDefaultMargin"
        android:layout_marginStart="@dimen/halfDefaultMargin"
        android:layout_marginEnd="@dimen/halfDefaultMargin"
        app:cardCornerRadius="@dimen/cornerRadius"
        >

    <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

        <TextView
                android:id="@+id/rVProf_pName"
                android:layout_width="300dp"
                android:layout_height="wrap_content"
                tools:text="Profilname"
                android:textSize="28sp"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                android:layout_marginStart="@dimen/defaultMargin"
                android:layout_marginTop="@dimen/DoubleDefaultMargin"
                >

        </TextView>

        <TextView
                android:id="@+id/rVProf_dayDose"
                android:layout_width="wrap_content"
                android:layout_height="0dp"
                tools:text="16"
                android:textSize="28sp"
                android:textStyle="bold"
                android:textAlignment="center"
                app:layout_constraintStart_toEndOf="@id/rVProf_pName"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                android:layout_marginTop="@dimen/DoubleDefaultMargin"
                android:layout_marginEnd="@dimen/defaultMargin"
                >

        </TextView>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>
